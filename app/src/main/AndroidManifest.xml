<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="luolc.training.interviewdemo" >

    <!-- permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/demo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HoleActivity"
            android:label="@string/hole_list_title"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".FileDownloadActivity"
            android:label="@string/file_download_title"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".ImageAsyncActivity"
            android:label="@string/image_async_title"
            android:screenOrientation="portrait" >
        </activity>

    </application>

</manifest>
